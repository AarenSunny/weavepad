{
  "name": "weavepad",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first collaborative editor built on a sequence CRDT.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --run test"
  }
}
